# Pin record for the vendored create-project-system snapshot.
# Machine-readable (key: value). Human context lives in ../README.md.
# Do not hand-edit to bump — follow the bump procedure in ../README.md.

repo:      git@github.com:cornjacket/create-project-system.git
tag:       v0.2.0
tag-type:  annotated
sha:       322924c4b58d15b9815b8dabb0471f2773a89d53
vendored:  generate.sh src/
excluded:  tests/ tasks/ docs/ PLAN.md daily-plan.md .claude/ .github/ self-test.sh README.md
manifest-sha256: 8ec2230900656285a8619684fe1bcf4d19fb223b387bf51328301ae57bcd19af

# manifest-sha256 is computed over the snapshot only (PIN itself excluded):
#   find generate.sh src -type f | LC_ALL=C sort | xargs shasum -a 256 | shasum -a 256

# --- Layer-1 contract guaranteed by v0.2.0 ---------------------------------
# generate.sh accepts exactly these ten flags and no others (verified against
# the vendored parser at generate.sh:58-80):
#
#   --target-repo --tasks-dir --epic --with-status --with-skill
#   --inject-claude-md --with-worktrees --with-projects --with-worktree-guard
#   --force
#
# Generation is non-destructive and re-runnable: machinery is overwritten,
# task content is never touched. The core / all-layers / project golden
# fixtures reproduce byte-for-byte at this ref.
#
# There is NO --with-pipeline. The generator never emits pipeline machinery.

# --- Changed from v0.1.0 ---------------------------------------------------
# --with-classes was RENAMED to --with-worktrees. The old spelling now exits
# non-zero naming its replacement, rather than aliasing: generate-time flags
# are typed once, during a deliberate upgrade. Flag count is unchanged at ten.
#
# setup-project.sh does not pass --with-classes, so this repo's call site was
# unaffected by the rename.
#
# Snapshot grew 41 -> 42 files:
#   src/layers/classes/classes.md  ->  src/layers/worktrees/worktrees.md
#   src/scripts/set-field.sh       ->  new (sets Tags/Priority post-creation)
#
# NO MIGRATION IS REQUIRED for the Category -> Worktree rename. Metadata rows
# and classes.md are content, which the generator never rewrites, so old
# spellings are permanent state and the readers accept both permanently:
# list-tasks.sh falls back from "| Worktree |" to "| Category |", both scripts
# fall back from worktrees.md to classes.md, and generate.sh seeds worktrees.md
# only when neither name exists — even under --force.
