# Status — 2026-03-09

## Completed

**651a51 — add-project-management-system-template** (closed)

- Built `target/` skeleton: `setup-project.sh`, `init-claude-md.sh`, `verify-setup.sh`, `SETUP.md`, all task management scripts and templates
- Separated task and subtask templates — subtask Status is binary (`—` / `complete` only)
- Wrote `roles/PROJECT_MANAGER.md` defining PM responsibilities, decision rules, and handoff protocol
- Rewrote `ai-builder/orchestrator.py` with PM mode: `--job`, `--target-repo`, `--epic`, `--request` flags, conditional routing table, `current-job.txt` state file
- Renamed pipeline documents from "task" to "job" (`JOB-TEMPLATE.md`, `JOBS_READY`, `ALL_DONE`) to avoid collision with the task management system
- Rewrote `ai-builder/FLOW.md` with PM and non-PM mode flow diagrams
- Added end-to-end regression test (`tests/regression/template-setup/`) — 12 sections, all passing
- Refactored: `verify-setup.sh` is a standalone read-only checker; `test.sh` installs then calls it

## In Progress

None — clean slate.

## Up Next (backlog)

- `6fdb3a` — consolidate role definitions (move ARCHITECT.md etc. into `roles/`)
- `d9c12f` — review orchestrator routing and flow

## Drafted

- `37a660` — design oracle and pipeline phases (8 subtasks)
- `284735` — design orchestrator output directory
- `702059` — add start-role flag to orchestrator
