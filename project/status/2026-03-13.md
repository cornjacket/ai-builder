# Session: 2026-03-13

## Work Completed

### `d5dad2` — Decomposition regression test (subtask of `8eea17`)
All four sub-subtasks complete:
- `ceca3e` — scaffold: `tests/regression/user-service/` directory layout, `reset.sh`, `README.md`
- `b7f3a1` — job document template: `JOB-user-service.md.template` (service-build format, no components pre-specified)
- `e2d4c8` — gold test suite: `gold/gold_test.go` — HTTP behavioural tests, language-agnostic, robust to different decompositions
- `f9a5b2` — ran pipeline end-to-end, all 8 gold tests passed

### Bugs found and fixed during pipeline run
1. **`agent_wrapper.py`** — multi-turn claude responses didn't have newlines between text blocks, breaking `^OUTCOME:` regex parsing
2. **`orchestrator.py`** — ARCHITECT received hardcoded `valid_outcomes = "DONE | NEED_HELP"` regardless of job doc type; now detects from `## Components` / `## Design` content
3. **`orchestrator.py`** — TM mode ignored Oracle-seeded `current-job.txt` at startup; now reads it to initialise `job_doc` for the first ARCHITECT call
4. **`reset.sh`** — didn't clean previously generated `JOB-*.md` files from output dir

### Documentation
- `orchestrator.md` — updated Main Loop section (always starts at ARCHITECT), File Reads/Writes table, Oracle contract documented
- `open-questions.md` — removed resolved "gold verifier for service-level regression tests" question

## Work In Progress

- `8eea17-implement-decomposition-in-orchestrator` — one subtask remains: `3cbd2e-702059-add-start-role-flag`

## Next Up

1. **`3cbd2e`** — implement `--start-role` flag on orchestrator
2. **Design corrections identified this session** (to be tasked tomorrow):
   - `main` should not be an ARCHITECT-decomposed component — it's entry-point scaffolding, not a domain component
   - Service-build job doc needs an Acceptance Criteria section for service-level end-to-end testing
   - ARCHITECT prompt + `JOB-component-design` template need language-agnostic unit test directive ("every component must include a unit test file; framework from project CLAUDE.md")
   - Task README = job document (merge): `JOB-service-build` → parent task README, `JOB-component-design` → subtask README; no separate JOB.md files; `current-job.txt` points to task README
   - Output dir should default to `<target-repo>/project/pipeline-output/`; wrapper script or Oracle convention to set it deterministically

## Key Decisions

- **Oracle contract**: before invoking the orchestrator, Oracle writes the job doc path to `current-job.txt` in the output dir. Orchestrator reads this at startup to initialise `job_doc` for the first ARCHITECT call. Documented in `orchestrator.md`.
- **`main` is not a component**: entry-point wiring is scaffolding, not a domain decomposition decision. ARCHITECT should decompose domain components only.
- **Task README = job document**: the task README at every level (parent = service-build, subtask = component-design) should be the job document. No separate JOB.md files. Not yet implemented — agreed in principle.
- **DOCUMENTER not yet implemented**: no README or companion `.md` files are generated for pipeline output. `DOCS:` field not parsed. Noted as backlog.
