# Session: 2026-03-14

## Work Completed

- **Closed `8eea17-implement-decomposition-in-orchestrator`** — all subtasks
  done; task moved to `complete/`.

- **Pipeline restructure (`de4588`) fully landed** — 9 subtasks complete:
  - Role-prefixed outcomes (`ARCHITECT_DESIGN_READY`, `TM_SUBTASKS_READY`, etc.)
  - Unified task template (replaces separate task/subtask templates)
  - New scripts: `set-current-job.sh`, `subtasks-complete.sh`, `is-last-task.sh`
  - TM prompt rewrite: two branches on `ARCHITECT_DECOMPOSITION_READY` and
    `TESTER_TESTS_PASS`; TM sets `Last-task: true` on final subtask
  - ARCHITECT prompt: Decompose Mode always adds `integrate` as final component;
    Design Mode triggered by `Complexity == atomic`
  - Updated `IMPLEMENTOR.md`, `TESTER.md`, `routing.md`, orchestrator docs
  - Retired standalone `JOB-*.md` templates

- **User-service regression test (`d5dad2`)** — pipeline runs end-to-end
  decomposing a Go HTTP CRUD service into `store` / `handlers` / `integrate`
  components; gold tests 8/8 passing.

- **Regression tests moved from `/tmp` to `sandbox/`** — `template-setup`
  and `user-service` reset/test scripts updated; `gold_test.go` computes paths
  from repo root via `os.Getwd()`.

- **`wont-do-subtask.sh` added** — sets `Status: wont-do` in subtask README
  and removes entry from parent subtask list; directory kept for reference.
  Deployed to both `project/tasks/scripts/` and `target/project/tasks/scripts/`.
  Documented in `CLAUDE.md`, task READMEs, and `verify-setup.sh`.

- **`3cbd2e-702059-add-start-role-flag-to-orchestrator`** — marked wont-do
  (superseded by `2faff3-add-configurable-start-state-and-routes`).

- **All agents switched back to `claude`** in `orchestrator.py` after Gemini
  hit 429 capacity limits.

- **Redefined task system types (`f7a6af`)** — three typed task types:
  USER-TASK, USER-SUBTASK, PIPELINE-SUBTASK; each with its own template and
  creation script. Replaced `new-task.sh` with `new-user-task.sh`,
  `new-user-subtask.sh`, `new-pipeline-subtask.sh`. Added `new-epic.sh`,
  `new-project.sh`, `list-projects.sh`. Documented hierarchy rules in
  `CLAUDE.md`, `project/tasks/README.md`, `target/project/tasks/README.md`,
  and `init-claude-md.sh`. Updated `verify-setup.sh`.

- **Closed `d9c12f-review-orchestrator-routing-and-flow`** — fixed stale docs
  (agents table, `JOB-TEMPLATE.md` deleted, oracle ref removed, `gemini` →
  `claude` in `routing.md`); marked DOCUMENTER hook planned/unimplemented;
  documented TM prompt inline design decision; updated TM prompt to call
  `new-pipeline-subtask.sh`; deleted `new-task.sh`.

- **Fixed `init-claude-md.sh` syntax error** — heredoc inside `$()` broke on
  apostrophe in `pipeline's` and backtick code fences; rewritten to use a
  temp file.

- **Re-ran user-service regression test** — pipeline completed clean
  (`TM_ALL_DONE`), gold tests pass.

- **Created `0838a5-tm-tree-traversal`** [HIGH, draft] — covers both the
  flat-case parent-not-closed bug and composite decomposition as the same
  root problem (TM has no tree-traversal logic).

## Work In Progress

Nothing currently in-progress.

## Next Up

1. `0838a5-tm-tree-traversal` [HIGH, draft] — design TM tree traversal:
   mark leaf complete, walk up, mark parent complete when all siblings done,
   find next sibling, signal `TM_ALL_DONE` at root
2. `2da360-document-flexible-task-system-usage` [HIGH, draft]
3. `37a660-design-oracle-and-pipeline-phases` [HIGH, draft]
4. `f1b8a0-establish-ai-builder-documentation` [HIGH, draft]
5. `3e0310-design-documenter-agent` [HIGH, draft]
6. `2faff3-add-configurable-start-state-and-routes-to-orchestrator` [MED, backlog]

## Key Decisions

- **Three task types**: USER-TASK (top-level, no Parent, no pipeline sections),
  USER-SUBTASK (human-owned, has Parent, no pipeline sections), PIPELINE-SUBTASK
  (pipeline-owned, has Parent, has pipeline sections). Separate scripts per type
  rather than one generic `new-task.sh`.

- **TM prompt stays inline**: not extracted to `roles/TASK_MANAGER.md` because
  it has two distinct branches with deeply embedded runtime values. Documented
  in `orchestrator.md`.

- **Sandbox for all regression artefacts**: target repos and output dirs for
  regression tests must go under `sandbox/`, never `/tmp`. Added to `CLAUDE.md`.

- **Parent-not-closed is a known bug**: after `TM_ALL_DONE` the parent service
  task remains `in-progress`. Tracked as `0838a5-tm-tree-traversal` along with
  the composite decomposition gap — both are the same root issue.
