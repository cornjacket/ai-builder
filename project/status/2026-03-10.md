# Status — 2026-03-10

## Completed

No tasks closed today — work was design and brainstorming.

## Work Done

**verify-setup.sh refactor**
- Split `target/verify-setup.sh` into a standalone read-only structural
  checker (directories, scripts, templates, CLAUDE.md, GEMINI.md)
- Rewrote `tests/regression/template-setup/test.sh` to install, call
  `verify-setup.sh`, then run workflow tests — 12 sections, all passing
- Updated `tests/regression/template-setup/README.md` to match

**Design discussions — CLAUDE.md hierarchy and agent operation**

Key decisions and insights captured across several tasks:

- `3230ef-design-workspace-claude-md-boundary` — added planning-vs-design
  distinction (CLAUDE.md placement is a per-job planning decision, not a
  setup-script concern), ARCHITECT ownership, bootstrapping problem, and
  review checkpoint requirement
- `2c2130-design-subagent-cwd-convention` (new subtask under `37a660`) —
  subagents must be spawned from the correct CWD for the CLAUDE.md hierarchy
  to load correctly; instructing a running agent to "work in a subdirectory"
  does not trigger re-traversal
- `dcdf24-add-stop-after-to-subtask-template` — clarified that `Stop-after`
  is exceptional, not default; ARCHITECT sets it only for high-risk or
  irreversible architectural decisions; common case is automatic pipeline flow
- `9b9d18-design-reviews-directory` — review process does not use the
  pipeline; it is a human checkpoint; CLAUDE.md placement is a required
  review checklist item for any job introducing new directories

**Brainstorm: Effective AI Agent Operation**
- Created `sandbox/brainstorm-effective-ai-agent-operation.md`
- Five principles: context management, instruction hierarchy (CLAUDE.md),
  documentation hierarchy (README.md), structured handoffs, task granularity
- Central thread: agents are stateless and context-limited — the system
  around them must carry the structure, memory, and correctness guarantees
  that a human developer would hold in their head
- Includes open questions and a path to promotion into `docs/` and role
  definitions

## Up Next (backlog)

- `d9c12f` — review orchestrator routing and flow [HIGH]
- `6fdb3a` — consolidate role definitions [MED]

## Drafted / In Flight

- `37a660` — design oracle and pipeline phases [HIGH] — 9 subtasks
  (added `2c2130-design-subagent-cwd-convention` today)
- `3230ef` — design workspace CLAUDE.md boundary [MED]
- `f1b8a0` — establish ai-builder documentation [HIGH]
