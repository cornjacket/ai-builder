# Task: update-claude-md-and-companion-doc

| Field       | Value                  |
|-------------|------------------------|
| Task-type   | USER-SUBTASK           |
| Status      | —                      |
| Epic        | main               |
| Tags        | —               |
| Parent      | 8ec43a-list-tasks-by-category             |
| Priority    | —           |
| Created     | 2026-05-02            |
| Completed   | —                      |
| Next-subtask-id | 0000               |

## Goal

1. Add `--category` and `--group-by-category` to the `list-tasks.sh`
   examples in `ai-builder/CLAUDE.md` (Task Management → Scripts section
   and the "outstanding work" examples block).
2. Update `project/tasks/scripts/list-tasks.md` (companion doc) to
   describe the two new flags. Create the doc if it doesn't exist.
3. Update `project/tasks/scripts/README.md` if it references the flag
   surface of `list-tasks.sh`.

## Context

Final subtask per the documentation rule. Do not run `complete-task.sh`
on the parent until this subtask is done.

## Subtasks

<!-- When a subtask is finished, run complete-task.sh --parent to mark it [x] before moving on. -->
<!-- subtask-list-start -->
<!-- subtask-list-end -->

## Notes

_None._
